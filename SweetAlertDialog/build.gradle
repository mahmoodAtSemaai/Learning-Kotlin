apply plugin: 'com.android.library'
apply from: rootProject.file('library-common-module.gradle')

android {

    lintOptions {
        abortOnError false
    }
}
dependencies {
    api(Dependencies.materialishProgress)
}